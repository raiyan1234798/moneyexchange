{
  "$schema": "https://developers.cloudflare.com/pages/platform/build-configuration/",
  "comment": "Reference config for Cloudflare Pages (unimoni). Set these in Dashboard → Workers & Pages → unimoni → Settings → Builds.",
  "projectName": "unimoni",
  "legacyProjectName": "moneyexchange",
  "frameworkPreset": "Next.js (Static HTML Export) or None",
  "rootDirectory": "web",
  "buildCommand": "npm ci && npm run build",
  "buildOutputDirectory": "out",
  "nodeVersion": "22",
  "environmentVariables": {
    "NEXT_PUBLIC_FIREBASE_API_KEY": "Firebase Console → Project settings → Your apps → Web app",
    "NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN": "moneyexchange-35c33.firebaseapp.com",
    "NEXT_PUBLIC_FIREBASE_PROJECT_ID": "moneyexchange-35c33",
    "NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET": "moneyexchange-35c33.firebasestorage.app",
    "NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID": "From Firebase web app config",
    "NEXT_PUBLIC_FIREBASE_APP_ID": "From Firebase web app config",
    "NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID": "Optional — Google Analytics",
    "NEXT_PUBLIC_R2_UPLOAD_URL": "Optional — R2 upload worker URL after deploy (see scripts/R2-SETUP.md)"
  },
  "notes": [
    "Rename temp-app to unimoni in the dashboard, or create a new Pages project named unimoni.",
    "Root directory MUST be web so npm ci installs Next.js and dependencies.",
    "Do not use repo root as build root — only root package.json installs there and web/node_modules is missing.",
    "After changing env vars, trigger a new deployment from the dashboard or push to main."
  ]
}
