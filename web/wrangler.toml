# Cloudflare Pages — used when deploying from web/ via Wrangler CLI.
# Git-connected builds: set root directory to web in the dashboard (see ../cloudflare.json).
name = "unimoni"
compatibility_date = "2024-12-01"
pages_build_output_dir = "out"
